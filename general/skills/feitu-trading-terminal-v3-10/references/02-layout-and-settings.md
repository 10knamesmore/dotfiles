# 客户端布局与设置

# 客户端布局

初次登录客户端后，会有操作指引，介绍系统功能的5个主要模块。

一是我的，支持操作所有交易账户的登录和查询资金及持仓明细。

> **界面 7：操作指引-我的**
> 「客户端主界面 > 操作指引第 1 步」浮层卡片（叠加在「账户管理」页面之上）。布局：
> - 背景层（被指引浮层半透明遮蔽）：顶部蓝色 tab「账户管理」激活、右侧 × 关闭；下方「交易账户管理」子页 +「证券账户/信用账户」tab；账户表格列「交易账户名称、交易账户ID、可用资金、持仓市值、资产户、选择登录的账户、登录状态、操作」，行示例「普通测试 / 1111111 / 80,000,000.00 / 20,824,371.00 / 133,750,093.00 / 登录 / 登录成功 / 下单设置 设为默认账户」；下方「持仓/委托/成交/赎回」tab + 持仓明细表（贵州茅台、中兴通讯、同花顺等示例行）。
> - 左上 1.【指引卡片】（蓝色圆角浮层，蓝底白字）：
>   - 标题大字「我的」。
>   - 副标题小字「查询所有交易账户的资金及持仓信息」。
>   - 卡片中部白底圆角小标签矩阵（5 个浅蓝胶囊按钮）：第一行「保存登录账户信息」（橙色高亮）、「持仓管理」（橙色高亮）；第二行「交易账户管理」「交易账户登录」「交易账户登录」；第三行（白色）三个空白胶囊。
> - 卡片左下「跳过指引 (1/5)」灰色文字链接。
> - 卡片右下蓝色主按钮「下一步」。
> 与正文配合：对应正文「一是我的，支持操作所有交易账户的登录和查询资金及持仓明细」。

二是智能交易，包含拆单交易，底仓增强交易和拆单结算等多个功能页，支持算法交易，监控中心，数据导入/导出等交易功能。

> **界面 8：操作指引-智能交易**
> 「客户端主界面 > 操作指引第 2 步」浮层卡片。布局：
> - 背景层：与界面 7 相同的「账户管理」页面。
> - 左侧 1.【指引卡片】（蓝色圆角浮层）：
>   - 标题「智能交易」。
>   - 副标题小字「以Rust优势构建的高效稳定的智能交易系统，搭载行业领先的非凸算法」。
>   - 卡片中部小标签矩阵（5 个浅蓝胶囊）：第一行「拆单交易」「监控中心」「拆单结算」「绩效分析」；第二行「特别接入方式」（橙色高亮）、「智能算法」（橙色高亮）。
> - 卡片左下「跳过指引 (2/5)」灰字。
> - 卡片右下两个按钮：白底「上一步」、蓝底「下一步」。
> 与正文配合：对应正文「二是智能交易，包含拆单交易，底仓增强交易和拆单结算等多个功能页」。

三是设置弹窗按钮，包含交易账户，版本和安全等设置。

> **界面 9：操作指引-设置**
> 「客户端主界面 > 操作指引第 3 步」浮层卡片。布局：
> - 背景层：「账户管理」页面。
> - 左下 1.【指引卡片】（蓝色圆角浮层，位于客户端左下角，靠近齿轮图标）：
>   - 标题「设置」+ 副标题小字「对系统进行设置」。
>   - 卡片内 4 行图标 + 文字列表（白底圆角行）：
>     - 「交易账户」配人像图标，描述「查看与管理交易账户下单业务和交易设置」。
>     - 「本地设置」配文件图标，描述「查看修改本地工作目录，扫单等本地设置」。
>     - 「版本」配方块图标，描述「查看版本详细信息和更新」。
>     - 「安全设置」配锁图标，描述「管理修改账户描述信息和安全设置」。
> - 卡片底部「跳过指引 (3/5)」灰字 + 白底「上一步」+ 蓝底「下一步」。
> 与正文配合：对应正文「三是设置弹窗按钮，包含交易账户，版本和安全等设置」。

四是用户设置弹窗按钮，包含交易账户设置，修改密码和退出登录功能。

> **界面 10：操作指引-用户账号**
> 「客户端主界面 > 操作指引第 4 步」浮层卡片。布局：
> - 背景层：「账户管理」页面。
> - 左下 1.【账户卡片浮层】（白底圆角弹出卡片）：顶部蓝色头像（打码）+ 账户名（打码）；下方三行菜单——「交易账户设置」「修改密码」「退出登录」（带退出图标，最底行）。
> - 右下 2.【指引卡片】（蓝色圆角浮层）：标题「用户账号界面」+ 副标题小字「显示用户账户的基本信息和账户用户名登录等功能」。
> - 卡片底「跳过指引 (4/5)」灰字 + 白底「上一步」+ 蓝底「下一步」。
> 与正文配合：对应正文「四是用户设置弹窗按钮，包含交易账户设置，修改密码和退出登录功能」。

五是表格自定义列表设置功能，包含列表设置，恢复默认和隐藏列等功能。

> **界面 11：操作指引-自定义列表**
> 「客户端主界面 > 操作指引第 5 步（最后一步）」浮层卡片。布局：
> - 背景层：「账户管理」页面。
> - 中部 1.【指引卡片】（蓝色圆角浮层，居中显示）：
>   - 标题「所有表格新增自定义列表设置功能」。
>   - 副标题小字「右键表头列名称，即可执行以下操作：【列表设置】【恢复默认列表设置】【隐藏列】」。
>   - 卡片中部白底图示区：示例表头右键菜单——「✓ 全部列名」「✓ 设置默认列表设置」「✓ 隐藏当前列」「列表设置」四行选项（带红色箭头指向某行）。
> - 卡片底「跳过指引 (5/5)」灰字 + 白底「上一步」+ 蓝底「完成」（高亮）。
> 与正文配合：对应正文「五是表格自定义列表设置功能，包含列表设置，恢复默认和隐藏列等功能」，红字「完成」按钮结束指引。

# 设置

操作指引结束后，会自动弹出本地设置窗口。

## 本地设置

在1.【目录设置】中，我们可以修改工作目录路径。工作目录是指非凸智能交易终端的输入输出数据(如母单扫描目录、持仓和子单导出目录等)的父目录。

注意：如果您依赖V3.0.0以前老版本客户端扫单上传母单或者持仓导出等自动化功能，请指定工作目录到老客户端的目录以避免修改程序，也可以修改您的程序将母单上传到当前工作目录下对应子目录。

在2.【导出设置】中，我们可以打开/关闭子单和持仓的实时导出功能和设置持仓导出间隔。

在3.【文件扫单】中，我们可以打开/关闭扫单下单前的二次确认功能。

> **界面 12：设置-本地设置**
> 「设置 > 本地设置」弹窗（整体）。布局：
> - 顶部标题栏：左侧「设置」标题，右侧关闭 ×。
> - 顶部 tab 区（水平居中）：四个 tab（图标 + 文字）——「交易账户」（人像图标）、「本地设置」（文件图标，深色背景，当前选中态）、「版本」（方块图标）、「安全设置」（锁图标）。
> - 左侧导航栏（三个分类）：
>   - 1.【目录设置】：蓝底白字高亮（当前选中），右侧红色大字编号 **1**。
>   - 2.【导出设置】：灰色文字，右侧红色大字编号 **2**。
>   - 3.【文件扫单】：灰色文字，右侧红色大字编号 **3**。
> - 右侧主区（目录设置内容）：
>   - 第一行「指定工作目录」标签 + 输入框（值「C:\非凸智能交易终端」，后段打码）+ 文件夹图标按钮 + 问号帮助图标。
>   - 中部子项目说明（多行灰色文字）：
>     - `csv_to_do`: 拆单算法母单上传扫描监控目录
>     - `csv_done`: 拆单算法母单上传扫描备份目录
>     - `t0_csv_to_do`: 底仓增强算法母单上传扫描监控目录
>     - `t0_csv_done`: 底仓增强算法母单上传扫描备份目录
>     - `position_csv_to_do`: 目标持仓扫单母单上传扫描监控目录
>     - `export`: 每日和实时交易数据导出目录
>   - 底部粉色提示框（红字）：「提示：如果您依赖V3.0.0以前老版本客户端扫单上传母单或者持仓导出等自动化功能，请指定工作目录到老客户端的目录以避免修改程序，也可以修改您的程序将母单上传到当前工作目录下对应子目录」。
> 与正文配合：对应正文「在1.【目录设置】中，我们可以修改工作目录路径」「在2.【导出设置】中」「在3.【文件扫单】中」三处编号引用。

## 交易账户

### 证券账户

选择证券交易账户进行下单和交易设置。

> **界面 13：交易账户-证券账户列表**
> 「设置 > 交易账户 > 证券账户」页面（整体）。布局：
> - 顶部 tab 区：「交易账户」（人像图标，当前选中，深色背景）、「本地设置」、「版本」、「安全设置」。
> - 左侧二级导航：
>   - 「证券账户」蓝底白字高亮（当前选中）。
>   - 「信用账户」灰字。
> - 右侧主区：
>   - 顶部搜索框「交易账户名称/交易账户ID」+ 放大镜图标。
>   - 表格列头「交易账户名称」「交易账户ID」「操作」（带升降排序箭头）。
>   - 表格数据行：「普通测试」「1111111」+ 操作列三个蓝色文字链接「下单设置 | 拆单设置 | 底仓增强设置」。
>   - 「操作」列上方有红色大字编号 **1**（对齐「下单设置」）、**2**（对齐「拆单设置」）、**3**（对齐「底仓增强设置」）。
> 与正文配合：对应正文 1.【下单设置】、2.【拆单设置】、3.【底仓增强设置】三处编号引用。

在1.【下单设置】页面中，设置内容根据交易账户的下游交易通道会有区别。完成设置后，点击【确认】保存。

> **界面 14：证券账户-下单设置弹窗**
> 「证券账户 > 操作 > 下单设置」弹窗（局部，模态框）。布局：
> - 顶部：标题「下单设置」+ 右侧关闭 ×。
> - 表单（自上而下）：
>   - 「交易类型」下拉框，值「模拟柜台」（灰色禁用态）+ ∨ 箭头。
>   - 「密码」输入框，已显示密码掩码点串。
>   - 「证券资金账号」空白输入框。
>   - 「连接指定的交易引擎IP+端口(不填默认为本机)」空白输入框。
> - 底部按钮组（右下）：白底「取 消」+ 蓝底「确 认」（外圈有红色矩形高亮框强调）。
> 与正文配合：对应正文「完成设置后，点击【确认】保存」，红框圈出「确认」按钮。

点击2.【拆单设置】弹出设置页面，勾选参数设置该账户下拆单母单的默认交易规则和算法。完成设置后，点击【确认】保存。

注意：以下参数仅对拆单算法母单生效。

超过时间后继续交易 ：母单超时后，未完成部分继续交易
上传母单后直接启动 ：确认状态下，母单上传后的状态默认为启动，在到达开始时间后自动开始交易。未确认状态下，母单上传后的状态默认为待启动，需要我们点击交易界面中的【启动】按钮后，才会在到达开始时间后进行交易
涨跌停继续交易 ：母单标的证券达到涨跌停后，未完成部分继续等待交易
最小下单金额 ：作为拆单子单委托最小金额的指导数值，子单委托金额预期大于设置值。当母单总金额过小时，拆单会减少子单总数，但子单委托金额可能会低于设置值
沪市日内最大委托笔数 ：单个交易日内，在上海交易所进行的拆单交易，子单下单次数须低于设置值
深市日内最大委托笔数 ：单个交易日内，在深圳交易所进行的拆单交易，子单下单次数须低于设置值

> **界面 15：证券账户-拆单设置弹窗**
> 「证券账户 > 操作 > 拆单设置」弹窗（局部，模态框）。布局：
> - 顶部：标题「拆单设置」+ 右侧账户标签（打码灰色块）+ 关闭 ×。
> - 表单（自上而下，6 行参数 + 默认算法）：
>   - 「超过时间后继续交易」空心复选框（未勾选）。
>   - 「上传母单后直接启动」蓝色实心勾选复选框（已勾选）。
>   - 「涨跌停继续交易」空心复选框（未勾选）。
>   - 「启动最小下单金额」空心复选框 + 帮助 ? + 数字步进器「− [空白] +」+ 单位「元」。
>   - 「沪市日内最大委托笔数」帮助 ? + 数字步进器「− 18000 +」+ 单位「次」。
>   - 「深市日内最大委托笔数」帮助 ? + 数字步进器「− 18000 +」+ 单位「次」。
>   - 「默认算法」下拉框，值「FT_AI_WAP_PLUS」+ 蓝色徽章「智能算法」+ ∨ 箭头。
> 与正文配合：对应正文「勾选参数设置该账户下拆单母单的默认交易规则和算法」六条参数列表项一一对应。

点击3.【底仓增强设置】弹出设置页面，选择该账户下底仓增强母单的默认算法。开启【最低评级】设置，仅允许上传大于等于当前评级设置的证券母单。点击【修改】设置策略可用资金。完成设置后，点击【确认】保存。

注意：策略可用资金限制账户进行底仓增强交易时最多可以占用的资金。
如不录入策略可用资金，策略可用资金默认为账户可用资金。修改策略可用资金在当日第一次下单后立即生效；如当日再次修改，则在次日第一次下单后生效。

> **界面 16：证券账户-底仓增强设置弹窗**
> 「证券账户 > 操作 > 底仓增强设置」弹窗（局部，模态框）。布局：
> - 顶部：标题「底仓增强设置」+ 账户标签「test」+ 关闭 ×。
> - 表单（自上而下）：
>   - 「默认算法」下拉框，值「FT_AI_T」+ ∨ 箭头。
>   - 「账户可用资金」灰色输入框，值「查询失败」（禁用态）。
>   - 「最低评级」帮助 ? + 五颗灰色空心五角星（默认未选评级，外圈有红色矩形高亮框强调）。
>   - 「策略可用资金」帮助 ? + 空白输入框 + 单位「元」+ 右侧白底带笔图标按钮「修改」（外圈有红色矩形高亮框强调）。
>   - 灰色提示「已生效的策略可用资金 无限制」。
>   - 黄色信息图标 + 灰字说明：「如不设置策略可用资金，策略可用资金默认为账户可用资金。修改策略可用资金在当日第一次下单后立即生效；如当日再次修改策略可用资金，则在次日第一次下单后生效」。
>   - 粉色风险提示框（红字）：「⚠ 当设置的策略可用资金超过账户可用时，策略执行有资金不足报错风险」「当策略可用资金设为较小数值时，策略执行若亏损有隔夜仓风险」。
> - 底部按钮组（右下）：白底「取 消」+ 蓝底「确 认」。
> 与正文配合：对应正文红框圈出「最低评级」和「修改」两处控件。

账户可用资金和生效策略可用资金会实时显示在下单窗口，点击【底仓增强设置】可以直接弹出【底仓增强设置】窗口。
通过文件上传的底仓增强交易母单的策略可用资金以客户端设置为准。

> **界面 17：下单窗口-底仓增强入口**
> 「智能交易 > 下单交易」窗口（局部，右侧下单面板）。布局：
> - 顶部标题「下单交易」+ 右上角紧凑/扩展切换图标。
> - 上方 tab 行：「文件上传」「母单上传」（蓝底白字选中态）「批量上传」。
> - 表单（自上而下）：
>   - 「交易账户」下拉框，值「普通测试」+ 绿色徽章「登录成功」+ ∨ 箭头。
>   - 「篮子名称」输入框，占位符「选填」。
>   - 「证券代码」输入框，占位符「代码」+ ∨ 箭头。
>   - 「算法类型」下拉框，值「FT_AI_T_PLUS」+ ∨ 箭头。
>   - 「时间范围」双输入框「开始时间 → 结束时间」+ 时钟图标。
>   - 「授权数量」输入框，占位符「股数」。
> - 资金信息行：「账户可用资金 80,000,000…」+ 「策略可用资金 无限制」（无限制三字外侧有红色斜向箭头指向）。
> - 蓝色齿轮文字链接「底仓增强设置」（左侧外圈有红色矩形高亮框强调）。
> - 底部红色主按钮「一键提交」。
> 与正文配合：对应正文「点击【底仓增强设置】可以直接弹出【底仓增强设置】窗口」（红框圈定）以及「策略可用资金」在下单窗口实时显示（红箭头指向）。

### 信用账户

选择信用交易账户进行下单和交易设置。

> **界面 18：交易账户-信用账户列表**
> 「设置 > 交易账户 > 信用账户」页面（整体）。布局：
> - 顶部 tab 区：「交易账户」选中态。
> - 左侧二级导航：
>   - 「证券账户」灰字。
>   - 「信用账户」蓝底白字高亮（当前选中）。
> - 右侧主区：
>   - 顶部搜索框「交易账户名称/交易账户ID」+ 放大镜。
>   - 表格列头「交易账户名称」「交易账户ID」「操作」（带排序箭头）。
>   - 表格数据两行：
>     - 「信用测试 / 222222 / 下单设置 | 拆单设置 | 底仓增强设置 | 自动还款设置」。
>     - 「信用测试2 / 333333 / 下单设置 | 拆单设置 | 底仓增强设置 | 自动还款设置」。
>   - 「操作」列上方有四个红色大字编号 **1**「下单设置」、**2**「拆单设置」、**3**「底仓增强设置」、**4**「自动还款设置」。
> 与正文配合：对应正文 1.【下单设置】、2.【拆单设置】、3.【底仓增强设置】、4.【自动还款设置】四处编号引用，并体现「信用账户」比「证券账户」多出一项「自动还款设置」。

1.【下单设置】和2.【拆单设置】与证券账户部分操作一致。
在3.【底仓增强设置】页面，新增显示【可用融资额度】，【账户可用保证金】和【策略可用融资】。

注意：策略可用融资限制信用账户进行融资底仓增强交易时的最大多头敞口。对应信用账户的策略可用资金仅限制进行担保品交易时的最大多头敞口。
如不录入策略可用融资，策略可用融资默认为账户可用融资额度和可用保证金的较小值。修改策略可用融资在当日第一次下单后立即生效；如当日再次修改，则在次日第一次下单后生效。

> **界面 19：信用账户-底仓增强设置弹窗**
> 「信用账户 > 操作 > 底仓增强设置」弹窗（局部，模态框）。布局：
> - 顶部：标题「底仓增强设置」+ 账户标签「对接FTS信用」+ 关闭 ×。
> - 表单（自上而下，比证券账户多出 3 项融资字段）：
>   - 「默认算法」下拉框（空白）+ ∨。
>   - 「账户可用资金」灰色输入框，值「查询失败」（禁用态）。
>   - 「最低评级」帮助 ? + 五颗灰色空心五角星。
>   - 「策略可用资金」帮助 ? + 空白输入框 + 单位「元」+「修改」按钮 + 灰字「已生效的策略可用资金 无限制」。
>   - 「可用融资额度」帮助 ? + 灰色禁用输入框（外圈有红色矩形高亮框强调）。
>   - 「账户可用保证金」灰色禁用输入框（外圈有红色矩形高亮框强调）。
>   - 「策略可用融资」帮助 ? + 输入框 + 单位「元」+「修改」按钮 + 灰字「已生效的策略可用融资 无限制」（外圈有红色矩形高亮框强调）。
>   - 黄色信息图标 + 多行灰字说明：「如不设置策略可用资金，策略可用资金默认为账户可用资金。如不设置策略可用融资，策略可用融资默认为账户可用融资额度和可用保证金的较小值。修改策略可用资金/融资在当日第一次下单后生效；如当日再次修改策略可用资金，则在次日第一次下单后生效」。
>   - 粉色风险提示框（红字）：「⚠ 当策略可用资金/融资设置为低于对应委托市值5%的较小数值时，策略执行会受限，若亏损有隔夜仓风险」。
> - 底部按钮组（右下）：白底「取 消」+ 蓝底「确 认」。
> 与正文配合：对应正文新增显示的三项【可用融资额度】、【账户可用保证金】、【策略可用融资】（三处红框圈定）。

可用融资额度，账户可用保证金和策略可用融资同样会实时显示在下单窗口，通过文件上传的底仓增强交易母单的策略可用融资以客户端设置为准。

在4.【自动还款设置】页面，点击5.【盘中还款】开启盘中自动还款。若设置预留最小可用资金，则实际还款金额 = MIN [ 需还款金额，(实际可用资金 - 账户预留可用资金) ]

注意：盘中自动还款仅对底仓增强模块存在启动中母单时生效。

> **界面 20：信用账户-自动还款设置弹窗**
> 「信用账户 > 操作 > 自动还款设置」弹窗（局部，模态框）。布局：
> - 顶部：标题「自动还款设置」+ 账户标签「信用测试」+ 关闭 ×。
> - 表单：
>   - 第一行「盘中还款」标签 + 右侧蓝色开关（已开启状态，蓝底白圆）；开关左侧红色大字编号 **5**。
>   - 第二行「预留最小可用资金*」（红色星号必填标识）+ 输入框（占位符「请输入」）。
>   - 黄色信息图标 + 灰字说明：「盘中自动还款仅对底仓增强模块存在启动中母单时生效，系统将于交易日盘中为您执行全额还款操作，系统将依据您所设置的账户预留可用资金，为您保留相应金额。」+「实际还款金额=min[需还款金额，(实际可用资金-账户预留可用资金)]」。
> - 底部按钮组（右下）：白底「取 消」+ 蓝底「确 认」。
> 与正文配合：对应正文「点击5.【盘中还款】开启盘中自动还款」（红色编号 **5** 指向开关）。

## 版本

点击1.【检查更新】，查询客户端当前版本，最新版本和更新说明。点击【立即更新】根据指引操作，更新完成后客户端自动退出重新登录。

> **界面 21：设置-版本-检查更新**
> 「设置 > 版本 > 检查更新」页面（整体）。布局：
> - 顶部 tab 区：「交易账户」「本地设置」「版本」（深色背景选中态，右上角有红色未读小圆点）「安全设置」。
> - 左侧二级导航：
>   - 1.【检查更新】：蓝底白字高亮（当前选中），外圈有红色矩形高亮框 + 右侧红色大字编号 **1**。
>   - 2.【关于】：灰字，外圈有红色矩形高亮框 + 右侧红色大字编号 **2**。
> - 右侧主区：
>   - 顶部：左侧蓝色软件图标 + 文字「检查到新版本V3.30.20」+「当前版本号：V3.10.0」。
>   - 中部「更新说明：」+ 大块白色文本框，内含「非凸智能交易终端[打码版本] V3.30.20带来以下更新：」（具体更新条目未展开显示）。
> - 底部按钮组（右下）：白底「稍后更新」+ 蓝底「立即更新」（外圈有红色矩形高亮框强调）。
> 与正文配合：对应正文「点击1.【检查更新】，查询客户端当前版本，最新版本和更新说明」与「点击【立即更新】根据指引操作」两处编号引用。

在2.【关于】页面中，查询当前客户端版本详细信息，支持自主下载客户端产品手册。

> **界面 22：设置-版本-关于**
> 「设置 > 版本 > 关于」页面（整体）。布局：
> - 顶部 tab 区：「版本」选中态（右上角红点）。
> - 左侧二级导航：
>   - 「检查更新」灰字。
>   - 「关于」蓝底白字高亮（当前选中）。
> - 右侧主区（垂直居中）：
>   - 大尺寸蓝色软件图标。
>   - 文字「非凸智能交易终端(」+ 打码版本号。
>   - 文字「版本：V3.」+ 打码尾号。
>   - 白底带下载箭头图标按钮「产品手册」（外侧有红色大箭头指向）。
>   - 底部分隔线下方居中小字「Copyright © 2018 非凸科技」。
> 与正文配合：对应正文「在2.【关于】页面中，查询当前客户端版本详细信息，支持自主下载客户端产品手册」，红箭头指向「产品手册」下载按钮。

## 安全设置

点击1.【修改密码】，输入旧密码和新密码，密码强度要求至少为一般及以上。点击 【更新密码】完成密码修改。修改成功后客户端自动退出，需要重新登录。

在2.【修改描述】页面中，可以查看修改当前用户的描述信息。

在3.【自动锁屏】页面中，可以修改自动锁屏设置。

> **界面 23：安全设置-修改密码**
> 「设置 > 安全设置 > 修改密码」页面（整体）。布局：
> - 顶部 tab 区：「交易账户」「本地设置」「版本」「安全设置」（深色背景选中态）。
> - 左侧二级导航：
>   - 1.【修改密码】：蓝底白字高亮（当前选中），右侧红色大字编号 **1**。
>   - 2.【修改描述】：灰字，右侧红色大字编号 **2**。
>   - 3.【自动锁屏】：灰字，右侧红色大字编号 **3**。
>   - 4.【自动启动】：灰字，右侧红色大字编号 **4**。
> - 右侧主区（修改密码表单）：
>   - 「* 旧密码」红色星号必填标识 + 输入框（占位符「请输入旧密码」）+ 右侧眼睛切换可见图标。
>   - 「* 新密码」必填 + 输入框（已输入三个掩码点）+ × 清空 + 眼睛图标；右侧「密码强度」标签 + 五格强度条（第一格红色填充，其余灰色空白）+ 灰字「非常弱」。
>   - 红字提示「当前密码强度：非常弱，密码强度至少为一般，请重新设置」（右侧有红色大箭头指向）。
>   - 「* 确认新密码」必填 + 输入框（占位符「请再次输入新密码」）+ 眼睛图标。
>   - 蓝色主按钮「更新密码」。
> 与正文配合：对应正文 1.【修改密码】、2.【修改描述】、3.【自动锁屏】三处编号引用；红箭头强调密码强度要求至少为一般及以上。

在4.【自动启动】页面中，可以设置客户端5.【开机自动启动】和6.定时【自动启动】。

注意：每日零点，系统会自动初始化并关闭客户端。
自动启动是打开登录窗口，需要窗口中的【自动登录】处于勾选状态，才会自动登录客户端。
定时自动启动最多可以设置五个启动时间。

> **界面 24：安全设置-自动启动**
> 「设置 > 安全设置 > 自动启动」页面（局部 + 悬浮提示）。布局：
> - 左侧二级导航（沿用界面 23 结构）：
>   - 「修改密码」灰字。
>   - 「修改描述」灰字。
>   - 「自动锁屏」灰字。
>   - 「自动启动」蓝底白字高亮（当前选中）。
> - 右侧主区（自上而下）：
>   - 顶部橙色提示条：橙色叹号图标 +「每日0点，系统会自动初始化并关闭客户端」。
>   - 5.【开机自动启动】行：空心复选框（未勾选）+ 文字「开机自动启动」+ 帮助 ? 图标；复选框左侧红色大字编号 **5**。该问号图标当前悬停，弹出黑色 tooltip 卡片，内容「开机时系统自动打开客户端登录窗口，若窗口中的【自动登录】处于勾选状态，会自动登录客户端。」
>   - 6.【自动启动】行：空心复选框（未勾选）+ 文字「自动启动」+ 帮助 ? 图标；复选框左侧红色大字编号 **6**。
>   - 「设置自动启动时间」标题下五行时间选择器：「设置一 / 设置二 / 设置三 / 设置四 / 设置五」+ 每行「请选择时间」占位 + 时钟图标。
> 与正文配合：对应正文「设置客户端5.【开机自动启动】和6.定时【自动启动】」两处编号引用，并印证「定时自动启动最多可以设置五个启动时间」（设置一到设置五五行表单）。
