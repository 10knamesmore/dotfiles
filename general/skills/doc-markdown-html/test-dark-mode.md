# 黑暗模式测试文档

这是一个测试文档，用于验证黑暗模式/白天模式切换功能。

## 功能特性

- 一键主题切换
- 自动系统主题检测
- 本地存储用户偏好
- 平滑的颜色过渡

## 代码示例

```python
def hello_world():
    """这是一个示例函数"""
    print("Hello, World!")
    return True

class Example:
    def __init__(self, name: str):
        self.name = name
    
    def greet(self) -> str:
        return f"Hello, {self.name}!"
```

## 引用示例

> 这是一个引用块，在黑暗模式下应该有合适的背景色和文字颜色。

## 列表示例

1. 第一项
2. 第二项
3. 第三项

- 无序列表项 A
- 无序列表项 B
- 无序列表项 C

## 表格示例

| 功能 | 状态 | 说明 |
|------|------|------|
| 主题切换 | ✅ 已实现 | 支持黑暗/白天模式 |
| 自动检测 | ✅ 已实现 | 跟随系统设置 |
| 本地存储 | ✅ 已实现 | 记住用户选择 |
| 平滑过渡 | ✅ 已实现 | 300ms 动画效果 |

## 行内代码

使用 `print()` 函数输出内容，或者使用 `class` 定义类。

## 总结

这个文档展示了黑暗模式切换功能在各种元素上的表现。