allowBuilds:
  '@google/genai': set this to true or false
  protobufjs: set this to true or false
# 这里只为编辑期取 .d.ts，不需要任何依赖的构建产物。
# pnpm 11 默认 strictDepBuilds=true：有未审阅的构建脚本就以非零码退出，
# 会连带打断 `pnpm exec`（它跑命令前先做 deps status check）。
strictDepBuilds: false
